import type { RunRecord } from '../../../features/run/data/runRepo';

export const saveRun = async (run: RunRecord): Promise<void> => {
  void run;
};

export const listRuns = async (): Promise<RunRecord[]> => {
  return [];
};
